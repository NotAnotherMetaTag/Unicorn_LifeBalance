<!--this code will display all html files in the current directory --!>
<?PHP
			
foreach (glob("*.html") as $filename) {
	   echo "<hr>\n"; 
	   $filecontents = file_get_contents($filename); 
       echo "$filecontents";
}
